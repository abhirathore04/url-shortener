# =============================================================================
# Docker Compose Environment Configuration Template
# Copy this file to .env.docker and replace with actual values
# =============================================================================

# MongoDB Configuration
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change_this_mongodb_admin_password
MONGO_USERNAME=urlshortener
MONGO_PASSWORD=change_this_mongodb_user_password

# Redis Configuration  
REDIS_PASSWORD=change_this_redis_password

# Application Security
JWT_SECRET=change_this_jwt_secret_key_at_least_32_characters_long

# Grafana Configuration
GRAFANA_ADMIN_PASSWORD=change_this_grafana_admin_password

# Management Tools (Development Only)
MONGO_EXPRESS_USER=admin
MONGO_EXPRESS_PASSWORD=change_this_mongoexpress_password

# Service Configuration
COMPOSE_PROJECT_NAME=url-shortener
COMPOSE_FILE=docker-compose.yml

# =============================================================================
# Instructions:
# 1. Copy this file: cp .env.docker.example .env.docker
# 2. Replace all "change_this_*" values with secure passwords
# 3. Never commit .env.docker to git (it contains secrets)
# =============================================================================
